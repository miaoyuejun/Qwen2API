{
  "installCommand": "npm install && cd public && npm install && npm run build",
  "buildCommand": null,
  "outputDirectory": null,
  "devCommand": null,
  "framework": "express",
  "functions": {
    "src/server.js": {
      "maxDuration": 60
    },
    "api/**/*.js": {
      "maxDuration": 60
    }
  }
}
